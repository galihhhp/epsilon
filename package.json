{
  "name": "challenge6",
  "version": "1.0.0",
  "description": "challenge6",
  "main": "index.js",
  "scripts": {
    "start": "nodemon src --exec babel-node",
    "sequelize": "sequelize",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "db:migrate": "sequelize db:migrate",
    "db:seed:all": "sequelize db:seed:all",
    "init:mig": "sequelize init:migrations",
    "init:mod": "sequelize init:models",
    "init:seed": "sequelize init:seeders",
    "gen:mig": "sequelize migration:generate",
    "gen:mod": "sequelize model:generate",
    "gen:seed": "sequelize seed:generate",
    "db:migrate:refresh": "sequelize db:migrate:undo:all && yarn db:migrate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Galih",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "ejs": "^3.1.5",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1",
    "pg": "^8.5.1",
    "sequelize": "^6.3.5"
  },
  "devDependencies": {
    "@babel/cli": "^7.12.1",
    "@babel/core": "^7.12.3",
    "@babel/node": "^7.12.1",
    "@babel/plugin-proposal-class-properties": "^7.12.1",
    "@babel/plugin-transform-runtime": "^7.12.1",
    "@babel/preset-env": "^7.12.1",
    "babel-eslint": "^10.1.0",
    "nodemon": "^2.0.6",
    "sequelize-cli": "^6.2.0"
  }
}